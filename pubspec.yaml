name: crypto_from_public_api
description: "A new Flutter project."
publish_to: 'none'
version: 0.1.0

environment:
  sdk: ^3.5.3

dependencies:
  flutter:
    sdk: flutter
  talker_flutter: ^4.4.1
  get_it: ^8.0.0
  auto_route: ^9.2.2
  http: ^1.2.2
  flutter_bloc: ^8.1.6
  url_launcher: ^6.3.0

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^4.0.0
  auto_route_generator: ^9.0.0
  build_runner: ^2.4.12

flutter:
  uses-material-design: true
